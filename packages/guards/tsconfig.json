{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "composite": true, // 👈 MUST be true for the workspace link to work
    "incremental": true,
    "types": [] // 👈 No Node or React types in Universal Guards
  },
  "include": [
    "src/**/*"
  ],
  "exclude": [
    "dist",
    "**/__tests__/*"
  ]
}