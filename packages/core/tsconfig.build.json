{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "src", // 🎯 Restrict back to src ONLY for production builds
    "composite": false
  },
  "exclude": [
    "__tests__/**/*",
    "**/*.test.ts",
    "**/*.spec.ts"
  ]
}